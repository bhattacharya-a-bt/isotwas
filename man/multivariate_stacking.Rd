% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate_stacking.R
\name{multivariate_stacking}
\alias{multivariate_stacking}
\title{Multivariate Super Learner Stacking}
\usage{
multivariate_stacking(
  X,
  Y,
  Y.rep = NULL,
  R = NULL,
  id = NULL,
  Omega = NULL,
  base_methods = c("multi_enet", "univariate_enet", "ridge", "lasso"),
  nfolds_stack = 5,
  verbose = FALSE,
  seed = 123,
  parallel = FALSE,
  n.cores = 1
)
}
\arguments{
\item{X}{matrix, design matrix of SNP dosages}

\item{Y}{matrix, matrix of G isoform expression across columns}

\item{Y.rep}{matrix, matrix of G isoform expression with replicates (optional)}

\item{R}{int, number of replicates (optional)}

\item{id}{vector, vector of sample ids (optional)}

\item{Omega}{matrix, precision matrix of Y (optional, computed if not provided)}

\item{base_methods}{character vector, methods to use as base learners.
Available: "multi_enet", "univariate_enet", "ridge", "lasso", "spls"}

\item{nfolds_stack}{int, number of CV folds for stacking weights}

\item{verbose}{logical}

\item{seed}{int, random seed}

\item{parallel}{logical, use parallel processing for base methods}

\item{n.cores}{int, number of cores for parallel processing}
}
\value{
isotwas_model object with stacked predictions
}
\description{
Combines predictions from multiple base methods using optimal weights
learned via cross-validation. This meta-learner approach typically
improves upon the best single method.
}
