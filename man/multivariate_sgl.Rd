% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate_sgl.R
\name{multivariate_sgl}
\alias{multivariate_sgl}
\title{Multivariate Sparse Group LASSO}
\usage{
multivariate_sgl(
  X,
  Y,
  alpha = 0.5,
  nlambda = 20,
  lambda_min_ratio = 0.01,
  nfolds = 5,
  standardize = FALSE,
  verbose = FALSE,
  seed = 123
)
}
\arguments{
\item{X}{matrix, design matrix of SNP dosages}

\item{Y}{matrix, matrix of G isoform expression across columns}

\item{alpha}{numeric, mixing parameter between group and individual sparsity (0-1)
alpha=1 is pure LASSO, alpha=0 is pure group LASSO}

\item{nlambda}{int, number of lambda values to try}

\item{lambda_min_ratio}{numeric, ratio of lambda_min to lambda_max}

\item{nfolds}{int, number of CV folds}

\item{standardize}{logical, standardize X before fitting}

\item{verbose}{logical}

\item{seed}{int, random seed}
}
\value{
isotwas_model object
}
\description{
Fits a sparse group LASSO model for multivariate isoform prediction.
This method encourages both:
- Group-level sparsity: same SNPs tend to affect multiple isoforms (shared effects)
- Within-group sparsity: isoform-specific effects
}
\details{
The penalty is: alpha * ||B||_1 + (1-alpha) * sum_j ||B_j||_2
where B_j is the j-th row of B (effects of SNP j across all isoforms)
}
